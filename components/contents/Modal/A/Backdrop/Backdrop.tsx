import Modal from '@/headless/Modal/Modal';
import styles from './Backdrop.module.scss';

const Backdrop = () => {
    return <Modal.Backdrop className={styles.Backdrop} />;
};

export default Backdrop;
